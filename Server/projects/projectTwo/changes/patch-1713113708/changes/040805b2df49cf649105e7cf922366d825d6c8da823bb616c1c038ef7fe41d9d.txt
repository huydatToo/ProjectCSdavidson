@@ -1,67 +1 @@
-// Define an array of cities
-const cities = ["New York", "London", "Tokyo", "Paris", "Sydney"];
-
-// Function to randomly select a city from the array
-function getRandomCity() {
-  const randomIndex = Math.floor(Math.random() * cities.length);
-  return cities[randomIndex];
-}
-citiesHllows
-// Define an object representing a person
-const person = {
-  name: "John",
-  age: 30,
-  city: getRandomCity(),
-  greet() {
-    console.log(`Hello! My name is ${this.name}. I'm ${this.age} years old and I live in ${this.city}.`);
-  }
-};
-
-// Call the greet method of the person object
-person.greet();
-
-
-// Define an array of cities
-const cities = ["New York", "London", "Tokyo", "Paris", "Sydney"];
-
-// Function to randomly select a city from the array
-function getRandomCity() {
-  const randomIndex = Math.floor(Math.random() * cities.length);
-  return cities[randomIndex];
-}
-
-// Define an object representing a person
-const person = {
-  name: "John",
-  age: 30,
-  city: getRandomCity(),
-  greet() {
-    console.log(`Hello! My name is ${this.name}. I'm ${this.age} years old and I live in ${this.city}.`);
-  }
-};
-
-// Call the greet method of the person object
-person.greet();
-
-
-// Define an array of cities
-const cities = ["New York", "London", "Tokyo", "Paris", "Sydney"];
-
-// Function to randomly select a city from the array
-function getRandomCity() {
-  const randomIndex = Math.floor(Math.random() * cities.length);
-  return cities[randomIndex];
-}
-
-// Define an object representing a person
-const person = {
-  name: "John",
-  age: 30,
-  city: getRandomCity(),
-  greet() {
-    console.log(`Hello! My name is ${this.name}. I'm ${this.age} years old and I live in ${this.city}.`);
-  }
-};
-
-// Call the greet method of the person object
-person.greet();
+itamar
\
